# Session 015 — Production-readiness audit

**Date:** 2026-09-01  
**Objective:** Complete the launch-readiness audit requested for the HMK platform and record the final verification state.

## Work completed

- Ran `npm run build` successfully; the production bundle generated 79 routes.
- Ran `npm run lint` successfully with no warnings or errors.
- Ran `npm run typecheck` successfully.
- Scanned `src/` for `console.*`, `TODO`, `FIXME`, and `debugger`; no matches were found.
- Audited the live Supabase catalog: all 78 public tables have RLS enabled, zero are disabled, and every table has at least one policy.
- Confirmed the service-role key remains confined to the server-only Supabase module; no non-public credential literals were found in client/source code.
- Reviewed the actual Next configuration (`next.config.ts`); no `next.config.mjs` exists.
- Added localized metadata and `metadataBase`, plus `robots.txt`, `sitemap.xml`, and the `NEXT_PUBLIC_SITE_URL` configuration contract.
- Replaced the theme-toggle mount effect with `useSyncExternalStore` to remove the final lint warning without changing the UX.

## Files changed

- `claude.md` — recorded Session 015, production-readiness status, and the authenticated-E2E caveat.
- `.env.example` — documents `NEXT_PUBLIC_SITE_URL` and existing runtime/E2E placeholders.
- `src/app/[locale]/layout.tsx` — localized canonical/Open Graph metadata and robots directives.
- `src/app/robots.ts` — crawler policy for public versus private routes.
- `src/app/sitemap.ts` — localized public route sitemap.
- `src/components/theme-toggle.tsx` — lint-safe external-store mount detection.

## Database and security

No schema or data changes were made. The live RLS inventory is 78/78 enabled with policies. Supabase advisor findings for intentional `SECURITY DEFINER` functions remain documented architectural exceptions: privileged transitions assert authorization internally, and anonymous certificate verification is required by BR-10.

## Verification results

| Check | Result |
|---|---|
| `npm run build` | PASS |
| `npm run lint` | PASS |
| `npm run typecheck` | PASS |
| Source debug-marker scan | PASS — no matches |
| Public-table RLS audit | PASS — 78 enabled, 0 disabled, 0 without policies |
| Authenticated E2E | Environment-gated; skips loudly without `E2E_*` credentials |

## Remaining launch actions

Set the real production value for `NEXT_PUBLIC_SITE_URL` and provide role-bearing `E2E_*` credentials when the authenticated editorial suite is rerun. Dynamic detail URLs may be added to the sitemap in a future content-focused pass.
