---
description: Open a work session on the HMK Platform — load memory, read the latest journal, confirm the goal
---

You are resuming work on the HMK AI & Robotics Club Platform.

Do these in order, without skipping:

1. Read `claude.md` in the project root. It is the anti-hallucination contract. If anything
   you believe about this project conflicts with it, the file wins.
2. Read the most recent file in `journals/` (sort by filename descending — they are
   `YYYY-MM-DD-session-NNN.md`).
3. Check the "Current Status" and "Open questions" sections of `claude.md` against what is
   actually on disk. If they disagree, say so — do not silently trust either one.
4. Report back, briefly:
   - where the last session ended
   - what is verified vs merely written (be exact; "written" is not "tested")
   - any blocking item waiting on the club
5. Then ask: **what is the goal for this session?**

Do not begin implementing anything before step 5 is answered.
